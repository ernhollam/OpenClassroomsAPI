<?xml version="1.0" encoding="UTF-8" ?>

<configuration scan="true"> <!-- scan for reconfiguration every minute -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <file>${LOG_PATH}/${LOG_FILE_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${LOG_FILE_NAME}-%d{yyyy-MM-dd}.zip</fileNamePattern>
            <maxHistory>30</maxHistory> <!-- keep 30 days -->
            <!-- total size of all archive files, if total size > 3GB, it will delete old archived file -->
            <totalSizeCap>3MB</totalSizeCap>
        </rollingPolicy>
    </appender>
    <property name="LOG_PATH" value="logs" />

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <property name="LOG_FILE_NAME" value="safetynetalerts" />

    <logger name="com.safetynet.safetynetalerts" additivity="false" level="DEBUG">
        <appender-ref ref="FILE" />
        <appender-ref ref="STDOUT" />
    </logger>

    <logger name="org.springframework.boot.web.embedded.tomcat" additivity="false" level="INFO">
        <appender-ref ref="FILE" />
    </logger>

    <root level="WARN">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="FILE" />
    </root>
</configuration>